const screen = []; const result = document.getElementById("result"); function clearDisplay() { result.value = ""; screen = []; } function display(value) { result.value += value; const number = result.value; screen.push(number); } const eraseOne = () => { screen.pop(); result.value = ""; const last = screen.length - 1; result.value += screen[last]; if (screen.length == 0) { result.value = ""; } }; function calculate() { const p = result.value; const q = eval(p); if (q < 10000001) { result.value = q; } else { result.value = "ERR"; } }